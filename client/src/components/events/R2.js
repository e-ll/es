import React from 'react';

export default function R2(props) {
  const re = []
  return {
    re.map((index)=>(<Tooltip title={`Стенд № ${0}`} interactive>
          <Link to={`/event/${0}`}>
            <div
              style={{
                position: "absolute",
                left: l,
                top: t,
                height: "3.95%",
                width: "auto",
              }}
              aria-label="delete"
            >
              <Estand
                logoUrl="https://www.logaster.ru/blog/wp-content/uploads/2018/02/google.png"
                name="Google"
              />
            </div>
          </Link>
        </Tooltip>))
    
  }

  
}