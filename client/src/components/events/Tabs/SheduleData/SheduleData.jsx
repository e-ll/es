import React from "react";
import SheduleDataStyles from './SheduleData.module.css';

///////////////////////// Styles /////////////////////////////
const {main} = SheduleDataStyles;

///////////////////////// Component /////////////////////////////
export const SheduleData = (props) => {
    return (
        <div className = {main}>
            
        </div>
    )
}